const JSXRules = () => {
  return (
    <div>
      <h1>JSX Rules</h1>
      <p>1. JSX must return a single parent element.</p>
      <p>2. JSX elements must be properly closed.</p>
      <p>3. JSX attributes are written using camelCase (e.g., className instead of class).</p>
    </div>
  )
}

export default JSXRules